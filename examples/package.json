{
	"type": "module",
	"scripts": {
		"build": "rm -rf dist html && tsc && npx whits dist/static html/static && npx whits -e dist/extend/whits.js dist/extend html/extend"
	},
	"dependencies": {
		"@types/node": "^20.9.1",
		"whits": "../"
	}
}
