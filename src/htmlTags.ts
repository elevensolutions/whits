import {_} from './index.js';


/**
 * Creates a new `a` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _a = _('a');
	
/**
 * Creates a new `applet` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _applet = _('applet');
	
/**
 * Creates a new `area` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _area = _('area');
	
/**
 * Creates a new `audio` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _audio = _('audio');
	
/**
 * Creates a new `base` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _base = _('base');
	
/**
 * Creates a new `basefont` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _basefont = _('basefont');
	
/**
 * Creates a new `blockquote` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _blockquote = _('blockquote');
	
/**
 * Creates a new `body` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _body = _('body');
	
/**
 * Creates a new `br` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _br = _('br');
	
/**
 * Creates a new `button` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _button = _('button');
	
/**
 * Creates a new `canvas` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _canvas = _('canvas');
	
/**
 * Creates a new `caption` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _caption = _('caption');
	
/**
 * Creates a new `col` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _col = _('col');
	
/**
 * Creates a new `colgroup` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _colgroup = _('colgroup');
	
/**
 * Creates a new `data` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _data = _('data');
	
/**
 * Creates a new `del` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _del = _('del');
	
/**
 * Creates a new `details` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _details = _('details');
	
/**
 * Creates a new `dialog` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _dialog = _('dialog');
	
/**
 * Creates a new `dir` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _dir = _('dir');
	
/**
 * Creates a new `div` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _div = _('div');
	
/**
 * Creates a new `dl` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _dl = _('dl');
	
/**
 * Creates a new `embed` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _embed = _('embed');
	
/**
 * Creates a new `fieldset` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _fieldset = _('fieldset');
	
/**
 * Creates a new `font` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _font = _('font');
	
/**
 * Creates a new `form` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _form = _('form');
	
/**
 * Creates a new `frame` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _frame = _('frame');
	
/**
 * Creates a new `frameset` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _frameset = _('frameset');
	
/**
 * Creates a new `h1` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _h1 = _('h1');
	
/**
 * Creates a new `h2` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _h2 = _('h2');
	
/**
 * Creates a new `h3` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _h3 = _('h3');
	
/**
 * Creates a new `h4` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _h4 = _('h4');
	
/**
 * Creates a new `h5` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _h5 = _('h5');
	
/**
 * Creates a new `h6` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _h6 = _('h6');
	
/**
 * Creates a new `head` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _head = _('head');
	
/**
 * Creates a new `hr` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _hr = _('hr');
	
/**
 * Creates a new `html` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _html = _('html');
	
/**
 * Creates a new `iframe` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _iframe = _('iframe');
	
/**
 * Creates a new `img` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _img = _('img');
	
/**
 * Creates a new `input` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _input = _('input');
	
/**
 * Creates a new `ins` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _ins = _('ins');
	
/**
 * Creates a new `isindex` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _isindex = _('isindex');
	
/**
 * Creates a new `label` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _label = _('label');
	
/**
 * Creates a new `legend` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _legend = _('legend');
	
/**
 * Creates a new `li` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _li = _('li');
	
/**
 * Creates a new `link` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _link = _('link');
	
/**
 * Creates a new `map` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _map = _('map');
	
/**
 * Creates a new `menu` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _menu = _('menu');
	
/**
 * Creates a new `meta` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _meta = _('meta');
	
/**
 * Creates a new `meter` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _meter = _('meter');
	
/**
 * Creates a new `object` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _object = _('object');
	
/**
 * Creates a new `ol` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _ol = _('ol');
	
/**
 * Creates a new `optgroup` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _optgroup = _('optgroup');
	
/**
 * Creates a new `option` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _option = _('option');
	
/**
 * Creates a new `output` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _output = _('output');
	
/**
 * Creates a new `p` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _p = _('p');
	
/**
 * Creates a new `param` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _param = _('param');
	
/**
 * Creates a new `pre` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _pre = _('pre');
	
/**
 * Creates a new `progress` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _progress = _('progress');
	
/**
 * Creates a new `q` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _q = _('q');
	
/**
 * Creates a new `script` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _script = _('script');
	
/**
 * Creates a new `select` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _select = _('select');
	
/**
 * Creates a new `slot` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _slot = _('slot');
	
/**
 * Creates a new `source` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _source = _('source');
	
/**
 * Creates a new `style` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _style = _('style');
	
/**
 * Creates a new `table` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _table = _('table');
	
/**
 * Creates a new `tbody` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _tbody = _('tbody');
	
/**
 * Creates a new `td` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _td = _('td');
	
/**
 * Creates a new `textarea` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _textarea = _('textarea');
	
/**
 * Creates a new `tfoot` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _tfoot = _('tfoot');
	
/**
 * Creates a new `th` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _th = _('th');
	
/**
 * Creates a new `thead` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _thead = _('thead');
	
/**
 * Creates a new `time` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _time = _('time');
	
/**
 * Creates a new `tr` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _tr = _('tr');
	
/**
 * Creates a new `track` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _track = _('track');
	
/**
 * Creates a new `ul` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _ul = _('ul');
	
/**
 * Creates a new `video` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _video = _('video');
	
/**
 * Creates a new `abbr` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _abbr = _('abbr');
	
/**
 * Creates a new `acronym` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _acronym = _('acronym');
	
/**
 * Creates a new `address` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _address = _('address');
	
/**
 * Creates a new `article` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _article = _('article');
	
/**
 * Creates a new `aside` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _aside = _('aside');
	
/**
 * Creates a new `b` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _b = _('b');
	
/**
 * Creates a new `bdi` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _bdi = _('bdi');
	
/**
 * Creates a new `bdo` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _bdo = _('bdo');
	
/**
 * Creates a new `bgsound` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _bgsound = _('bgsound');
	
/**
 * Creates a new `big` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _big = _('big');
	
/**
 * Creates a new `blink` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _blink = _('blink');
	
/**
 * Creates a new `center` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _center = _('center');
	
/**
 * Creates a new `cite` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _cite = _('cite');
	
/**
 * Creates a new `code` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _code = _('code');
	
/**
 * Creates a new `command` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _command = _('command');
	
/**
 * Creates a new `content` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _content = _('content');
	
/**
 * Creates a new `datalist` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _datalist = _('datalist');
	
/**
 * Creates a new `dd` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _dd = _('dd');
	
/**
 * Creates a new `dfn` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _dfn = _('dfn');
	
/**
 * Creates a new `dt` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _dt = _('dt');
	
/**
 * Creates a new `element` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _element = _('element');
	
/**
 * Creates a new `em` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _em = _('em');
	
/**
 * Creates a new `figcaption` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _figcaption = _('figcaption');
	
/**
 * Creates a new `figure` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _figure = _('figure');
	
/**
 * Creates a new `footer` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _footer = _('footer');
	
/**
 * Creates a new `header` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _header = _('header');
	
/**
 * Creates a new `hgroup` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _hgroup = _('hgroup');
	
/**
 * Creates a new `i` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _i = _('i');
	
/**
 * Creates a new `image` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _image = _('image');
	
/**
 * Creates a new `kbd` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _kbd = _('kbd');
	
/**
 * Creates a new `keygen` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _keygen = _('keygen');
	
/**
 * Creates a new `listing` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _listing = _('listing');
	
/**
 * Creates a new `main` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _main = _('main');
	
/**
 * Creates a new `mark` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _mark = _('mark');
	
/**
 * Creates a new `marquee` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _marquee = _('marquee');
	
/**
 * Creates a new `math` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _math = _('math');
	
/**
 * Creates a new `menuitem` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _menuitem = _('menuitem');
	
/**
 * Creates a new `multicol` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _multicol = _('multicol');
	
/**
 * Creates a new `nav` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _nav = _('nav');
	
/**
 * Creates a new `nextid` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _nextid = _('nextid');
	
/**
 * Creates a new `nobr` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _nobr = _('nobr');
	
/**
 * Creates a new `noembed` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _noembed = _('noembed');
	
/**
 * Creates a new `noframes` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _noframes = _('noframes');
	
/**
 * Creates a new `noscript` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _noscript = _('noscript');
	
/**
 * Creates a new `picture` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _picture = _('picture');
	
/**
 * Creates a new `plaintext` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _plaintext = _('plaintext');
	
/**
 * Creates a new `rb` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _rb = _('rb');
	
/**
 * Creates a new `rbc` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _rbc = _('rbc');
	
/**
 * Creates a new `rp` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _rp = _('rp');
	
/**
 * Creates a new `rt` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _rt = _('rt');
	
/**
 * Creates a new `rtc` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _rtc = _('rtc');
	
/**
 * Creates a new `ruby` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _ruby = _('ruby');
	
/**
 * Creates a new `s` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _s = _('s');
	
/**
 * Creates a new `samp` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _samp = _('samp');
	
/**
 * Creates a new `search` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _search = _('search');
	
/**
 * Creates a new `section` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _section = _('section');
	
/**
 * Creates a new `shadow` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _shadow = _('shadow');
	
/**
 * Creates a new `small` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _small = _('small');
	
/**
 * Creates a new `spacer` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _spacer = _('spacer');
	
/**
 * Creates a new `span` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _span = _('span');
	
/**
 * Creates a new `strike` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _strike = _('strike');
	
/**
 * Creates a new `strong` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _strong = _('strong');
	
/**
 * Creates a new `sub` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _sub = _('sub');
	
/**
 * Creates a new `summary` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _summary = _('summary');
	
/**
 * Creates a new `sup` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _sup = _('sup');
	
/**
 * Creates a new `svg` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _svg = _('svg');
	
/**
 * Creates a new `template` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _template = _('template');
	
/**
 * Creates a new `title` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _title = _('title');
	
/**
 * Creates a new `tt` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _tt = _('tt');
	
/**
 * Creates a new `u` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _u = _('u');
	
/**
 * Creates a new `var` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _var = _('var');
	
/**
 * Creates a new `wbr` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _wbr = _('wbr');
	
/**
 * Creates a new `xmp` `Tag` instance with the given attributes and content.
 * @param attributes The attributes of the tag.
 * @param content The children or content of the tag.
 * @returns A new `Tag` instance.
 */
export const _xmp = _('xmp');
	