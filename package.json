{
  "name": "htts",
  "version": "0.1.0",
  "description": "HyperText TypeScript Templating System",
  "author": "ELEVEN",
  "license": "MPL-2.0",
  "scripts": {
    "build": "rm -rf dist/* && npx tsc",
    "pretest": "cd tests && rm -rf dist/* && npx tsc",
    "test": "npx jest",
    "attr-types": "node convertTypes.mjs > src/htmlAttributes.ts"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "html-element-attributes": "^3.2.0",
    "jest": "^29.7.0",
    "typescript": "^5.2.2"
  }
}
